{
  "name": "airbyte-embedded-widget-demo",
  "private": true,
  "scripts": {
    "dev": "vite",
    "test": "pnpm dlx playwright install --with-deps chromium && playwright test",
    "test:ui": "pnpm dlx playwright install --with-deps chromium && playwright test --ui"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@playwright/test": "^1.42.1",
    "@types/node": "^22.13.14",
    "@vitejs/plugin-basic-ssl": "^2.0.0",
    "prettier": "^3.5.3",
    "typescript": "^5.3.3",
    "vite": "^5.0.0"
  }
}
